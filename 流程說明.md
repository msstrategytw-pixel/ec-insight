# 流程說明

本專案的分工、執行流程與修改流程。**流程有變動時請更新這份文件。**

最後更新：2026-08-02

---

## 一、分工

| 角色 | 負責 | 碰什麼 |
|------|------|--------|
| 顧問同事 | 決定「什麼算有價值的情報」 | 只碰 `編輯方針/` 五份方針；瀏覽網站 |
| 專案負責人 | 觸發執行、驗收內容品質 | 叫 agent 跑、看結果決定要不要調整 |
| Agent | 蒐集、評分、撰寫、發布 | 讀方針 → 產出 → push |
| GitHub | 存檔＋出網站（Pages） | 被動，無需維護 |

情報網站：https://msstrategytw-pixel.github.io/ec-insight/

## 二、執行一次的流程

```
① 對 Claude Code 說「依 WEEKLY_AGENT.md 跑一期」
        ↓
② Agent 讀 WEEKLY_AGENT.md（runbook，五個步驟）
        ↓
③ 讀 編輯方針/ 下的五份方針（repo 內即唯一來源）
        ↓
④ 蒐集：依方針執行 web 搜尋與網頁抓取
        ＋ 執行 scripts/fetch_amazon_rankings.py 抓 Amazon JP 榜單
        ↓
⑤ 篩選：依評分標準評分（總分 ≥9 刊登）、對照近幾期去重
        ↓
⑥ 產出三個檔案：
   docs/data/YYYY-MM-DD.json  （網站資料）
   docs/data/index.json        （加一筆期別）
   簡報/YYYY-MM-DD.md          （人讀版存檔）
        ↓
⑦ git commit + push
        ↓
⑧ GitHub Pages 自動部署 → 網站出現新一期
```

## 三、修改流程

### A. 調整選材、產業、評分標準（最常發生）

```
改 編輯方針/ 裡的 md 檔（GitHub 網頁編輯 / 請 AI 產出後貼上 / 交給工程端）
   → commit → 下次執行生效
```

不需通知任何人、不需等任何人。**「選材不準」的正確反應是去「評分標準」的範例庫加具體案例**，比修改抽象定義有效。詳細方式見 [編輯方針/README.md](編輯方針/README.md)。

> 大幅修改後可要求 agent「用新方針重跑上一期做對照」，看哪些會被刷掉、哪些會新增，確認方向再等正式執行。
> 每期執行都會 commit 當時的方針狀態，隨時可用 git 對照或還原。

### B. 調整網站版面、卡片欄位、tab 名稱

```
跟工程端說 → 改 repo 的 docs/（前端）→ push → 網站更新
```

### B2. 顧問對條目的回饋（閉環）

```
顧問在網站點「👍 有用 / 👎 沒用」（沒用可填一句原因）
        ↓ 送到 Apps Script 端點
存進 Google Sheet
        ↓ 下次執行時 agent 讀取
影響本期選材，並在編輯後記提出「建議加入評分標準範例庫」的案例
        ↓ 人工判斷採納後
寫進 編輯方針/評分標準.md → 永久影響往後選材
```

> 最後一哩維持人工判斷：agent 只**建議**哪些案例值得進範例庫，由人決定要不要收。

### C. 蒐集技術面問題（某來源爬不到、目標站改版）

```
agent 會在該期的「編輯後記」誠實記錄失敗原因
→ 回報工程端 → 修 scripts/ 或在方針中改用降級方案
```

## 四、目前未啟用

- **每週自動排程**：預定每週一 07:00 由雲端 agent 自動執行；尚未建立，目前為手動觸發。
- **網站存取限制**：repo 為 public、GitHub Pages 公開瀏覽，無登入保護。
